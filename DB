create database users;
use users;
create table member(id varchar(20) primary key, nickname varchar(20), name varchar(20), pw varchar(20), birthDate varchar(6), email varchar(50), pnumber varchar(11), home varchar(100));
