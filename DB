create database member;
use memeber;
create table user(id varchar(20) primary key, nickname varchar(20), name varchar(20), pw varchar(20), birthDate varchar(6), email varchar(50), pnumber varchar(11), home varchar(100));
